package ru.techno.testing.dao;
/*

import jakarta.persistence.*;
import lombok.AccessLevel;
import lombok.Getter;
import lombok.Setter;
import lombok.experimental.FieldDefaults;

import java.util.ArrayList;
import java.util.List;

@Entity
@Getter
@Setter
@Table(name = "candidate_choose_vacancy", schema = "test")
@FieldDefaults(level = AccessLevel.PRIVATE)
public class CandidateChooseVacancy extends BaseEntity {

    @ManyToMany(cascade = CascadeType.ALL)
    @JoinTable(name = "candidate_choose_vacancy", joinColumns = @JoinColumn(name="candidate_choose_vacancy_id",
    referencedColumnName = "candidate_choose_vacancy_id"), inverseJoinColumns = @JoinColumn(name = "candidate_id",
            referencedColumnName = "candidate_id"))
    List<Candidate> candidateList = new ArrayList<>();

    @ManyToMany(cascade = CascadeType.ALL)
    @JoinTable(name = "candidate_choose_vacancy", joinColumns = @JoinColumn(name="candidate_choose_vacancy_id",
            referencedColumnName = "candidate_choose_vacancy_id"), inverseJoinColumns = @JoinColumn(name = "vacancy_id",
            referencedColumnName = "vacancy_id"))
    List<Vacancy> vacancyList = new ArrayList<>();
*/

 // }
